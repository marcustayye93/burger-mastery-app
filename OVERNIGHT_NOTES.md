# Overnight Run Notes (v2.4.0 / v2.5.0 / v2.6.0)

User instruction: full autonomy, no further permission needed. Ship progressively:
1. v2.4.0 Steak Sides & Sauces (5 recipes) — DONE authoring: js/steak-sides-data.js (board sauce, compound butter, chimichurri, peppercorn, dauphinoise). Verified shape OK.
2. v2.5.0 Burger Essentials rewrite — DONE authoring: js/burger-lessons-data.js (6 chapters/32 lessons/160 slides after adding mb-blending + sc-doneness-safety). data.js now exports courses=burgerCourses, lessonSequence flattened with lessonId/lessonTitle, burgerLessons. Old storySlides kept as private const (not exported).
STATUS: v2.5.0 fully verified locally in browser: burger Learn 6 chapters/32 lessons/160 slides, story viewer advance + resume + full-completion (viewed:160, completed:6 chapters, profile 32/32 lessons); steak Flavours shows Condiments & sides (5 cards) and Herb Board Sauce story opens/steps/closes; steak side ids renamed to stk-side-*; sw.js bumped to v2.5.0 with new precache entries; state schema v5 migration resets burger progress. State reset to clean. Zero console errors. NEXT: commit+push v2.5.0, verify live, then build v2.6.0 Cook Journal.
Note: sandbox browser needed cache:reload fetches to pick up changed local files (python http.server heuristic caching) — not an app issue.
3. v2.6.0 Cook Journal — NOT started. Plan: post-cook outcome logging (nailed/overshot/undershot + note), stored in state.cookJournal[], shown in profile history; pull-temp suggestion on next cook of same protocol (e.g. overshot => suggest -2°C next time) in openCookSteak/steakBuilder region.

## Key integration facts
- App: /home/ubuntu/burger-mastery-app, local server on port 8901, deploy = git push origin main && git push origin main:gh-pages. Live: https://marcustayye93.github.io/burger-mastery-app/
- Current deployed version v2.3.0. sw.js: VERSION const at top + PRECACHE list — must add js/steak-sides-data.js and js/burger-lessons-data.js and bump VERSION per release.
- app.js imports (line 2): {courses,lessonSequence} from data.js; line 8: {sides,sideById} from sides-data.js.
- Steak Learn pattern to mirror for burger: app.js imports {steakCourses,steakLessons,steakLessonSequence,steakLessonCount} from steak-lessons-data.js; state.viewedSteakLessons[] (slide indices), state.steakStoryIndex; steakLearn() renderer w/ chapter cards + progress bars + Resume · n/160 button; openSteakStory(chapterId|null,resume) viewer; click wiring: [data-steak-story], [data-steak-resume]. Burger equivalents already exist as: openStory(sectionId|null,resume) using lessonSequence, markViewed(i), state.viewedLessons?? (check learn() function), [data-story], [data-resume]. state.completed via recomputeCompleted().
- Burger learn(): renders courses with chapterPct based on viewed slide ids — since lessonSequence slideIds changed format (lessonId-NN), old viewed arrays invalid → migration needed: bump schemaVersion to 5, reset state.viewedSlides/storyIndex for burger (keep steak).
- state stored in localStorage key "bm-state", schemaVersion currently 4, migration runner in app.js.
- Steak sides integration: app.js has openSide(sd.dataset.side) using sideById from sides-data.js; sides list rendered in Flavours (burger). For steak sides: add import {steakSides} from steak-sides-data.js; render a "Condiments & Sides" section in steak flavours view (steakFlavours region) with data-side handlers; extend sideById lookup or add steakSideById; open via openRecipeStory(side,{side:true}).
- Verify scripts: scripts/verify-burger-lessons.mjs (checks 6/32/160 + sides 5) — PASSES after lesson additions (rerun to confirm). scripts/verify-steak-lessons.mjs from v2.3.0.
- Burger profile: profile() shows lessons complete via state.completed/courses; check learn progress naming for burger — uses state.viewedLessons? verify in app.js learn() and recomputeCompleted().
- steak asset keys avail: stkBrine,stkMise,stkPanHeat,stkPanSear,stkBaste,stkGrill,stkOven,stkAirfry,stkReverseRack,stkSousvide,stkProbe,stkRest,stkButter,stkSlice,heroSteak; burger keys incl heroCross,heroSmash,heroBeefPorkPairing,heroBeefLambPairing,meatChicken*,onion*,garlic*,seasoning*,toast,sauce,layers,cheese,finished,portion,shaping,dimple,thickness,texture,mixing,grindLoose,pattyReady,bunPotatoToasted.
- img() helper in app.js maps raw github asset URLs to derived w480/w960 variants automatically (image cache versioned) — new data modules reuse assets.js keys so no new images needed.
- Commit style: "v2.X.0: description". After push, verify live via curl sw.js version + browser check.
- Engine canon: doneness 49/54/60/66/71°C; carryover pan/grill -5, oven -4, airfry -2, reverse -10 (no rest), sousvide 0; rests 5/8/10min by 2.5/4/5.5cm. Burger safety: pork blends 71°C, chicken 74°C.
